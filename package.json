{
  "name": "gitlab-status-monitor",
  "version": "0.1.21",
  "description": "Tray application for monitoring GitLab pipeline status with traffic lights",
  "main": "src/main.js",
  "author": {
    "name": "Jimmy Ahs",
    "email": "jimmyahs@gmail.com"
  },
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "test": "node --test test/config.test.js test/status.test.js",
    "bump:build-version": "npm version patch --no-git-tag-version",
    "build": "npm run bump:build-version && scripts/build-staged.sh --publish never",
    "build:mac": "npm run bump:build-version && scripts/build-staged.sh --mac dmg pkg --publish never",
    "build:win": "npm run bump:build-version && scripts/build-staged.sh --win nsis --publish never",
    "build:linux": "npm run bump:build-version && scripts/build-staged.sh --linux AppImage deb --publish never"
  },
  "devDependencies": {
    "electron": "^43.2.0",
    "electron-builder": "^26.15.3"
  },
  "build": {
    "appId": "se.jbitlabs.gitlab-status-monitor",
    "productName": "GitLab Status Monitor",
    "asar": true,
    "files": [
      "src/**/*",
      "package.json",
      "!**/._*",
      "!**/.DS_Store"
    ],
    "mac": {
      "category": "public.app-category.developer-tools",
      "icon": "build/icon.icns",
      "target": [
        {
          "target": "dmg",
          "arch": [
            "x64",
            "arm64"
          ]
        },
        {
          "target": "pkg",
          "arch": [
            "x64",
            "arm64"
          ]
        }
      ]
    },
    "win": {
      "icon": "build/icon.ico",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "linux": {
      "maintainer": "Jimmy Ahs <jimmyahs@gmail.com>",
      "icon": "build/icon.png",
      "target": [
        {
          "target": "AppImage",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "deb",
          "arch": [
            "x64"
          ]
        }
      ],
      "category": "Development"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
